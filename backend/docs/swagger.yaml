basePath: /
definitions:
  main.healthResponse:
    properties:
      status:
        example: ok
        type: string
    type: object
host: localhost:8080
info:
  contact: {}
  description: Multi-tenant event ticketing API for storefront, staff, and integration
    partners.
  title: Ticket POS API
  version: 1.0.0
paths:
  /health:
    get:
      description: Returns service health status.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/main.healthResponse'
      summary: Health check
      tags:
      - public
swagger: "2.0"
tags:
- description: Unauthenticated storefront routes
  name: public
- description: Authenticated staff routes
  name: staff
- description: Partner integration routes
  name: integration
