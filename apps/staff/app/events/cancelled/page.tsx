import { EventsSurface } from "../events-surface";

/** The Events list's Cancelled tab: every cancelled Event, however old. */
export default function CancelledEventsPage() {
  return <EventsSurface tab="cancelled" />;
}
