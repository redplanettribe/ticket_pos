import { EventsSurface } from "../events-surface";

/** The Events list's Past tab: the Events that are over and were not cancelled. */
export default function PastEventsPage() {
  return <EventsSurface tab="past" />;
}
